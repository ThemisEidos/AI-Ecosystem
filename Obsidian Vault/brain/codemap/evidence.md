---
title: evidence.py
tags:
  - codemap
loc: 281
---

# evidence.py

Workflow-evidence validation (Tests/Fixtures/Workflow_Evidence rule set).

Part of [[Code Map]] · `cooper-core/evidence.py` · 281 lines

## Functions

- `is_job_linked()` — —
- `is_completion()` — —
- `_schema_errors()` — —
- `_sensitive_errors()` — —
- `_verdicts_errors()` — Optional field: when present, 'verdicts' must be a non-empty list of
- `_find_approval()` — —
- `validate_completion()` — —
- `validate_approval()` — —
- `validate_record()` — —
- `_is_negative_fixture()` — True if `path` sits under an `Invalid/` directory inside `root`.
- `main()` — —
