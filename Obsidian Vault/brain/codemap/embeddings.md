---
title: embeddings.py
tags:
  - codemap
loc: 105
---

# embeddings.py

Embedding helpers for semantic selection (skills, and anything after them).

Part of [[Code Map]] · `cooper-core/embeddings.py` · 105 lines

## Functions

- `cosine()` — —
- `_hash()` — —
- `async embed_cached()` — Return one vector per text, fetching only cache misses (in input order).
- `make_fetcher()` — Async embed transport for the workshop's backend. 'openai' covers LiteLLM.
